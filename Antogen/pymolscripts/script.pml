
delete all
fetch , async = 0

# Blue -- Predus ()
# Red -- Annotated ()

color white; color blue, resi ; color red, resi  
color green, resi 

select color blue; show spheres, SEL
select color red; show spheres, SEL


remove resn hoh
zoom complete=1

# blue
orient resi  
#unset opaque_background

# removes the shadows that give the appearannce of depth
set depth_cue, 0  

center 


python

import pymolpredus

rotator = pymolpredus.Rotate("", "", "")
rotator.take_pictures()


python end

# the directory to which the png files are outputted
# png ../../Antogen/pymolimages/.png, width=900, height=900,ray=1, dpi=500



delete all

delete all
fetch , async = 0

# Blue -- Predus ()
# Red -- Annotated ()

color white; color blue, resi ; color red, resi  
color green, resi 

select color blue; show spheres, SEL
select color red; show spheres, SEL


remove resn hoh
zoom complete=1

# blue
orient resi  
#unset opaque_background

# removes the shadows that give the appearannce of depth
set depth_cue, 0  

center 


python

import pymolpredus

rotator = pymolpredus.Rotate("", "", "")
rotator.take_pictures()


python end

# the directory to which the png files are outputted
# png ../../Antogen/pymolimages/.png, width=900, height=900,ray=1, dpi=500



delete all

delete all
fetch , async = 0

# Blue -- Predus ()
# Red -- Annotated ()

color white; color blue, resi ; color red, resi  
color green, resi 

select color blue; show spheres, SEL
select color red; show spheres, SEL


remove resn hoh
zoom complete=1

# blue
orient resi  
#unset opaque_background

# removes the shadows that give the appearannce of depth
set depth_cue, 0  

center 


python

import pymolpredus

rotator = pymolpredus.Rotate("", "", "")
rotator.take_pictures()


python end

# the directory to which the png files are outputted
# png ../../Antogen/pymolimages/.png, width=900, height=900,ray=1, dpi=500



delete all

delete all
fetch , async = 0

# Blue -- Predus ()
# Red -- Annotated ()

color white; color blue, resi ; color red, resi  
color green, resi 

select color blue; show spheres, SEL
select color red; show spheres, SEL


remove resn hoh
zoom complete=1

# blue
orient resi  
#unset opaque_background

# removes the shadows that give the appearannce of depth
set depth_cue, 0  

center 


python

import pymolpredus

rotator = pymolpredus.Rotate("", "", "")
rotator.take_pictures()


python end

# the directory to which the png files are outputted
# png ../../Antogen/pymolimages/.png, width=900, height=900,ray=1, dpi=500



delete all

delete all
fetch , async = 0

# Blue -- Predus ()
# Red -- Annotated ()

color white; color blue, resi ; color red, resi  
color green, resi 

select color blue; show spheres, SEL
select color red; show spheres, SEL


remove resn hoh
zoom complete=1

# blue
orient resi  
#unset opaque_background

# removes the shadows that give the appearannce of depth
set depth_cue, 0  

center 


python

import pymolpredus

rotator = pymolpredus.Rotate("", "", "")
rotator.take_pictures()


python end

# the directory to which the png files are outputted
# png ../../Antogen/pymolimages/.png, width=900, height=900,ray=1, dpi=500



delete all

delete all
fetch , async = 0

# Blue -- Predus ()
# Red -- Annotated ()

color white; color blue, resi ; color red, resi  
color green, resi 

select color blue; show spheres, SEL
select color red; show spheres, SEL


remove resn hoh
zoom complete=1

# blue
orient resi  
#unset opaque_background

# removes the shadows that give the appearannce of depth
set depth_cue, 0  

center 


python

import pymolpredus

rotator = pymolpredus.Rotate("", "", "")
rotator.take_pictures()


python end

# the directory to which the png files are outputted
# png ../../Antogen/pymolimages/.png, width=900, height=900,ray=1, dpi=500



delete all

delete all
fetch , async = 0

# Blue -- Predus ()
# Red -- Annotated ()

color white; color blue, resi ; color red, resi  
color green, resi 

select color blue; show spheres, SEL
select color red; show spheres, SEL


remove resn hoh
zoom complete=1

# blue
orient resi  
#unset opaque_background

# removes the shadows that give the appearannce of depth
set depth_cue, 0  

center 


python

import pymolpredus

rotator = pymolpredus.Rotate("", "", "")
rotator.take_pictures()


python end

# the directory to which the png files are outputted
# png ../../Antogen/pymolimages/.png, width=900, height=900,ray=1, dpi=500



delete all

delete all
fetch , async = 0

# Blue -- Predus ()
# Red -- Annotated ()

color white; color blue, resi ; color red, resi  
color green, resi 

select color blue; show spheres, SEL
select color red; show spheres, SEL


remove resn hoh
zoom complete=1

# blue
orient resi  
#unset opaque_background

# removes the shadows that give the appearannce of depth
set depth_cue, 0  

center 


python

import pymolpredus

rotator = pymolpredus.Rotate("", "", "")
rotator.take_pictures()


python end

# the directory to which the png files are outputted
# png ../../Antogen/pymolimages/.png, width=900, height=900,ray=1, dpi=500



delete all

delete all
fetch , async = 0

# Blue -- Predus ()
# Red -- Annotated ()

color white; color blue, resi ; color red, resi  
color green, resi 

select color blue; show spheres, SEL
select color red; show spheres, SEL


remove resn hoh
zoom complete=1

# blue
orient resi  
#unset opaque_background

# removes the shadows that give the appearannce of depth
set depth_cue, 0  

center 


python

import pymolpredus

rotator = pymolpredus.Rotate("", "", "")
rotator.take_pictures()


python end

# the directory to which the png files are outputted
# png ../../Antogen/pymolimages/.png, width=900, height=900,ray=1, dpi=500



delete all

delete all
fetch , async = 0

# Blue -- Predus ()
# Red -- Annotated ()

color white; color blue, resi ; color red, resi  
color green, resi 

select color blue; show spheres, SEL
select color red; show spheres, SEL


remove resn hoh
zoom complete=1

# blue
orient resi  
#unset opaque_background

# removes the shadows that give the appearannce of depth
set depth_cue, 0  

center 


python

import pymolpredus

rotator = pymolpredus.Rotate("", "", "")
rotator.take_pictures()


python end

# the directory to which the png files are outputted
# png ../../Antogen/pymolimages/.png, width=900, height=900,ray=1, dpi=500



delete all

delete all
fetch , async = 0

# Blue -- Predus ()
# Red -- Annotated ()

color white; color blue, resi ; color red, resi  
color green, resi 

select color blue; show spheres, SEL
select color red; show spheres, SEL


remove resn hoh
zoom complete=1

# blue
orient resi  
#unset opaque_background

# removes the shadows that give the appearannce of depth
set depth_cue, 0  

center 


python

import pymolpredus

rotator = pymolpredus.Rotate("", "", "")
rotator.take_pictures()


python end

# the directory to which the png files are outputted
# png ../../Antogen/pymolimages/.png, width=900, height=900,ray=1, dpi=500



delete all

delete all
fetch , async = 0

# Blue -- Predus ()
# Red -- Annotated ()

color white; color blue, resi ; color red, resi  
color green, resi 

select color blue; show spheres, SEL
select color red; show spheres, SEL


remove resn hoh
zoom complete=1

# blue
orient resi  
#unset opaque_background

# removes the shadows that give the appearannce of depth
set depth_cue, 0  

center 


python

import pymolpredus

rotator = pymolpredus.Rotate("", "", "")
rotator.take_pictures()


python end

# the directory to which the png files are outputted
# png ../../Antogen/pymolimages/.png, width=900, height=900,ray=1, dpi=500



delete all
